# AI Node CLI Package

When working on a pre-existing codebase, it is important to understand the current code and the practices. This tool analyses and embeds all the repo information and enables chatting the with the codebase to better understand it and develop faster.

OpenAI's GPT 3.5 Turbo powers the chat functionality and Supabase is used as the vector database to store the embedding.
